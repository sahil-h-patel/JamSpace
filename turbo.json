{
  "$schema": "https://turborepo.com/schema.json",
  "globalEnv": ["VITE_SERVER_URL", "REDIS_URL", "PORT", "NODE_ENV"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}